{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('category_edit', {'id': category.id}) }}"><button class="btn-warning btn">Editer</button></a>
    <a href="{{ path('category_index') }}"><button type="submit" class="btn-primary btn">Retour à la liste</button></a>
    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
